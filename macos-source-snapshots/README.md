# macOS source snapshots

These ZIP files are the source snapshots available in the project workspace. They are included for reference and review. They are not a complete Xcode project by themselves. The release DMG must be built/tested separately on macOS before publication.
